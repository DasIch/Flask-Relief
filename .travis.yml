language: python

python:
  - 2.6
  - 2.7
  - pypy
  - 3.3

env:
  - REQUIREMENTS=lowest
  - REQUIREMENTS=release
  - REQUIREMENTS=devel

install:
  - pip install -r .travis-$REQUIREMENTS-requirements.txt
  - python setup.py test
  - pip install --use-mirrors tox
  - tox -e style,packaging
